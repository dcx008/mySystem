<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.boyuan.mySystem.mapper.UserInfoMapper">

    <!--配置别名包 否则不生效-->
    <!--配置别名包 否则不生效-->
    <insert id="addUserInfo">
        insert into user_info values(#{userInfoId},#{name},#{age},#{gender},#{address},#{telephone},#{email})
    </insert>

</mapper>